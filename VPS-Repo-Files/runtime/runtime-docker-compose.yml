version: "3.9"

services:
  l9_api:
    build:
      context: /opt/l9
      dockerfile: runtime/Dockerfile
    container_name: l9_api
    restart: unless-stopped
    env_file:
      - /opt/l9/.env
    ports:
      - "127.0.0.1:8000:8000"
