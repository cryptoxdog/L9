# =====================================
# GMP Agent Auto-Register (Spec)
# =====================================

filename: core/agent_registry_patch.py
type: module_spec
language: python
status: pending_implementation

description: |
  Allows GMP-defined agents to self-register on instantiation.
  Uses HandshakeResponse and AgentCapabilities to bind capabilities.

wiring:
  triggers:
    - meta.yaml ingestion
    - gmp_manifest.yaml
  binds:
    - tool capabilities
    - governance policy zone
    - audit feedback

code: |
  def register_gmp_agent(meta_path):
      import yaml
      meta = yaml.safe_load(open(meta_path))
      entry = {
          "agent_id": meta["name"],
          "tools": meta.get("tools", []),
          "governance_zone": meta.get("zone", "gmp"),
          "registered_by": "CodeGenAgent"
      }
      with open("config/agent_registry.yaml", "a") as f:
          yaml.dump([entry], f)
# ============================================================================
# L9 DORA BLOCK - AUTO-GENERATED - DO NOT EDIT
# ============================================================================
l9_dora:
  component_id: "AGE-INTE-031"
  component_name: "Core Agent Registry Patch"
  module_version: "1.0.0"
  created_at: "2026-01-08T03:17:26Z"
  created_by: "L9_DORA_Injector"
  layer: "intelligence"
  domain: "agent_execution"
  type: "config"
  status: "active"
  governance_level: "critical"
  compliance_required: true
  audit_trail: true
  purpose: "Configuration file for core agent registry patch"
  dependencies: []
# ============================================================================
# END L9 DORA BLOCK
# ============================================================================
