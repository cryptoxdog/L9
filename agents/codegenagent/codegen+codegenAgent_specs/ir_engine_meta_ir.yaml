# ================================
# L9 CodeGen Spec: Meta IR Schema
# ================================

filename: ir_engine/meta_ir.py
type: codegen_module
language: python

description: |
  Canonical IR class for representing a meta contract in structured form.
  Used as the foundation for all meta-driven codegen across L9.

wiring:
  imports:
    - from pydantic import BaseModel
    - from typing import List
  usage:
    - Used by compile_meta_to_ir
    - Consumed by CodegenAgent to emit module components

code: |
  from pydantic import BaseModel
  from typing import List

  class MetaContract(BaseModel):
      name: str
      description: str
      inputs: List[str]
      outputs: List[str]
      responsibilities: List[str]
      guarantees: List[str]
      required_docs: List[str]
      required_tests: List[str]
# ============================================================================
# L9 DORA BLOCK - AUTO-GENERATED - DO NOT EDIT
# ============================================================================
l9_dora:
  component_id: "AGE-INTE-040"
  component_name: "Ir Engine Meta Ir"
  module_version: "1.0.0"
  created_at: "2026-01-08T03:17:26Z"
  created_by: "L9_DORA_Injector"
  layer: "intelligence"
  domain: "agent_execution"
  type: "config"
  status: "active"
  governance_level: "critical"
  compliance_required: true
  audit_trail: true
  purpose: "Configuration file for ir engine meta ir"
  dependencies: []
# ============================================================================
# END L9 DORA BLOCK
# ============================================================================
