# Cursor MCP Memory Server Configuration
# Copy to .env.cursor and fill in your values
#
# cp env-cursor.example .env.cursor

# =============================================================================
# Required
# =============================================================================

# PostgreSQL connection string (l9memory database)
MEMORY_DSN=postgresql://l9user:YOUR_PASSWORD@localhost:5432/l9memory

# OpenAI API key for embeddings
OPENAI_API_KEY=sk-your-openai-key-here

# MCP API key for authentication (generate with: openssl rand -hex 32)
MCP_API_KEY=your-generated-mcp-api-key

# User identification for memory namespacing
USER_ID=your-user-uuid
CURSOR_USERNAME=your_cursor_username

# =============================================================================
# Optional (defaults shown)
# =============================================================================

# Server
MCP_HOST=127.0.0.1
MCP_PORT=9001
MCP_ENV=production
LOG_LEVEL=INFO

# Embedding model
OPENAI_EMBED_MODEL=text-embedding-3-small
OPENAI_EMBED_DIM=1536

# Memory lifecycle (hours)
MEMORY_SHORTTERM_HOURS=1
MEMORY_MEDIUMTERM_HOURS=24
MEMORY_CLEANUP_INTERVAL_MINUTES=60

# Retention for cleanup (days)
MEMORY_SHORT_RETENTION_DAYS=7
MEMORY_MEDIUM_RETENTION_DAYS=30

# Vector search defaults
VECTOR_SEARCH_THRESHOLD=0.7
VECTOR_SEARCH_TOP_K=10

