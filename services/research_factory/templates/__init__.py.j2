"""
{{ schema.system.system }}
{{ '=' * schema.system.system|length }}

{{ schema.system.role }}

Auto-generated by L9 Research Factory from schema v{{ schema.metadata.version }}.

Components:
- controller: Main agent controller
{% for file in schema.cursorinstructions.generatefiles %}
{% if file != 'controller.py' and file != '__init__.py' %}
- {{ file | replace('.py', '') }}: {{ file | replace('.py', '') | replace('_', ' ') | title }}
{% endif %}
{% endfor %}

Version: 1.0.0
"""

from {{ schema.system.rootpath | replace('/', '.') }}.controller import (
    {{ schema.system.name }}Controller,
    {{ schema.system.name }}Request,
    {{ schema.system.name }}Response,
    create_{{ schema.system.name | lower }}_controller,
)

__all__ = [
    "{{ schema.system.name }}Controller",
    "{{ schema.system.name }}Request",
    "{{ schema.system.name }}Response",
    "create_{{ schema.system.name | lower }}_controller",
]

